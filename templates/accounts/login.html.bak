{% extends "base_public.html" %}
{% load static %}
{% block title %}Connexion — Saveur 237{% endblock %}
{% block extra_head %}
<style>
:root {
  --glass: rgba(255,255,255,0.14);
  --glass-border: rgba(255,255,255,0.2);
  --blur: 16px;
  --radius: 20px;
  --shadow: 0 18px 60px rgba(0,0,0,0.25);
  --accent: #3E2723;
  --accent-2: #FCD116;
}
body{min-height:100vh;background:linear-gradient(rgba(0,0,0,0.45),rgba(0,0,0,0.45)),radial-gradient(circle at 20% 20%,rgba(252,209,22,0.22),transparent 40%),radial-gradient(circle at 80% 10%,rgba(0,135,81,0.18),transparent 35%),linear-gradient(135deg, rgba(62,39,35,0.65), rgba(0,0,0,0.75));}
.auth-shell{min-height:100vh;display:grid;place-items:center;padding:30px 12px;}
.auth-card{width:100%;max-width:480px;background:var(--glass);backdrop-filter:blur(var(--blur));-webkit-backdrop-filter:blur(var(--blur));border:1px solid var(--glass-border);border-radius:var(--radius);box-shadow:var(--shadow);padding:26px 26px 22px;color:#f7f7f7;animation:fadeUp .35s ease;}
.auth-logo{display:flex;align-items:center;gap:10px;margin-bottom:10px;}
.auth-logo img{width:44px;height:44px;border-radius:12px;object-fit:cover;border:1px solid rgba(255,255,255,0.2);} 
.auth-logo span{font-weight:800;letter-spacing:0.3px;}
.auth-card h1{margin:6px 0 4px;font-size:1.9rem;}
.auth-sub{color:rgba(255,255,255,0.82);margin:0 0 12px;}
.auth-field{display:grid;gap:6px;margin-bottom:12px;}
.auth-field label{font-weight:700;color:#f7f7f7;}
.auth-field input,.auth-field select{width:100%;padding:12px;border-radius:12px;border:1px solid rgba(255,255,255,0.28);background:rgba(255,255,255,0.08);color:#fff;}
.auth-field input:focus,.auth-field select:focus{outline:none;border-color:var(--accent-2);box-shadow:0 0 0 3px rgba(252,209,22,0.25);}
.password-wrap{position:relative;}
.password-wrap button{position:absolute;right:10px;top:50%;transform:translateY(-50%);border:none;background:transparent;color:#eee;cursor:pointer;}
.auth-actions{display:flex;gap:10px;flex-wrap:wrap;align-items:center;margin-top:6px;}
.btn-primary{background:var(--accent);color:#fff;padding:12px 16px;border-radius:12px;border:1px solid rgba(255,255,255,0.25);font-weight:800;text-decoration:none;}
.btn-primary:hover{background:#2f1d1a;}
.btn-secondary{color:#fff;padding:11px 14px;border-radius:12px;border:1px solid rgba(255,255,255,0.35);text-decoration:none;}
.footer-note{margin-top:10px;font-size:0.95rem;color:rgba(255,255,255,0.85);}
.small-link{color:var(--accent-2);text-decoration:none;}
.checkbox{display:flex;align-items:center;gap:8px;color:#f1f1f1;font-size:0.95rem;}
@keyframes fadeUp{from{opacity:0;transform:translateY(8px);}to{opacity:1;transform:translateY(0);} }
@media(max-width:640px){.auth-card{padding:22px 18px;border-radius:18px;}}
</style>
{% endblock %}
{% block content %}
<div class="auth-shell">
  <div class="auth-card">
    <div class="auth-logo">
      <img src="{% static 'logo/logo.png' %}" alt="Saveur 237">
      <span>Saveur 237</span>
    </div>
    <h1>Se connecter</h1>
    <p class="auth-sub">Ravi de vous revoir.</p>
    <form method="post">
      {% csrf_token %}
      {% if form.non_field_errors %}
        <div class="auth-field" style="color:#ffd2d2;">{{ form.non_field_errors }}</div>
      {% endif %}
      <div class="auth-field">
        <label for="id_username_or_email">Nom d’utilisateur ou Email</label>
        {{ form.username_or_email }}
        {{ form.username_or_email.errors }}
      </div>
      <div class="auth-field">
        <label for="id_password">Mot de passe</label>
        <div class="password-wrap">
          {{ form.password }}
          <button type="button" aria-label="Afficher le mot de passe" onclick="togglePwd('id_password', this)">👁</button>
        </div>
        {{ form.password.errors }}
      </div>
      <div class="auth-field">
        <label for="id_role">Type d’utilisateur</label>
        {{ form.role }}
        {{ form.role.errors }}
      </div>
      <div class="auth-actions">
        <label class="checkbox"><input type="checkbox" name="remember"> Se souvenir de moi</label>
        <a class="small-link" href="#">Mot de passe oublié ?</a>
      </div>
      <div class="auth-actions" style="margin-top:14px;">
        <button class="btn-primary" type="submit">Se connecter</button>
        <a class="btn-secondary" href="{% url 'accounts:register' %}">Créer un compte</a>
      </div>
      <p class="footer-note">Pas encore de compte ? <a class="small-link" href="{% url 'accounts:register' %}">Créer un compte</a></p>
    </form>
  </div>
</div>
<script>
function togglePwd(id,btn){const i=document.getElementById(id);if(!i)return;i.type=i.type==='password'?'text':'password';}
</script>
{% endblock %}
